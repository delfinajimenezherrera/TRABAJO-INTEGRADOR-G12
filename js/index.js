.then(function (response) {
for (let i=0; i<5; i++): 
    let peliculaTitulo= //deberiamos poner el documento entre [i] . la seccion donde se encuentran los nombres
    let fotoPelicula= //lo mismo que el anterior
    let fechaDeEstreno= //lo mismo que el anterior 
    //ahora abria que poner que se modifique en la pagina HTML
    finalHtml += `<section class="caja"> 
                <a href="./poner el vinculado del personaje)alt=""></a> 
                <p> Titulo: (poner la variable $pelicula titulo) </p>
                <p> Fecha de estreno: (poner la variable fechaDeEstreno) </p>
                <img> de la pelicula
                </section>`
}
finalHtml.innerHTML += contenidoTrack
    return response
